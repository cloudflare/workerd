using Workerd = import "/workerd/workerd.capnp";
using StrategiesModules = import "strategies-modules.capnp";

# Runs the queuing strategies suite against the TypeScript implementation,
# with byte-for-byte the same modules as strategies-cpp.wd-test. The flags
# pinned there are deliberately absent: the TypeScript implementation
# hard-codes the modern behaviors, and the `@` / `@all-compat-flags`
# variants verify that indifference with the flags all off and all on.

const unitTests :Workerd.Config = (
  services = [
    ( name = "streams-strategies-ts",
      worker = (
        modules = StrategiesModules.modules,
        compatibilityFlags = [
          "nodejs_compat",
          "typescript_implemented_streams",
          "experimental",
        ],
      )
    ),
  ],
  autogates = [
    "workerd-autogate-per-isolate-javascript-bootstrap",
  ],
);
