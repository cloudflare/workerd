load("@aspect_rules_ts//ts:defs.bzl", "ts_config", "ts_project")

ts_config(
  name = "node@tsconfig",
  src = ":tsconfig.json",
)

ts_project(
  name = "node",
  tsconfig= "node@tsconfig",
  srcs = glob(["**/*.ts"]),
)
