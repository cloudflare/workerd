load("//:build/wd_rust_test.bzl", "wd_rust_test")

wd_rust_test(
    name = "heap_buffer_overflow",
    srcs = ["heap_buffer_overflow.rs"],
    edition = "2024",
    target_compatible_with = select({
        "//build/platforms:sanitizer_address": [],
        "//conditions:default": ["@platforms//:incompatible"],
    }),
)
