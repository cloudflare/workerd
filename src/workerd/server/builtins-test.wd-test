using Workerd = import "/workerd/workerd.capnp";
using TestBundle = import "builtins-test-bundle.capnp";

const unitTests :Workerd.Config = (
  services = [
    ( name = "builtins-test",
      worker = (
        modules = [
          (name = "worker", esModule = embed "builtins-test.js")
        ],
        compatibilityDate = "2023-01-15",
        compatibilityFlags = ["nodejs_compat"],
      )
    ),
  ],
  extensions = [ TestBundle.bundle ],
);
