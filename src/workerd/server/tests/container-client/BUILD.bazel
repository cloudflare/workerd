load("//:build/wd_test.bzl", "wd_test")

wd_test(
    size = "enormous",
    src = "test.wd-test",
    args = ["--experimental"],
    data = ["test.js"],
    tags = ["off-by-default"],
)

wd_test(
    size = "enormous",
    src = "test-entrypoint.wd-test",
    args = ["--experimental"],
    data = ["test-entrypoint.js"],
    # tags = ["off-by-default"],  # Temporarily disabled for testing
)
