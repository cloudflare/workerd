using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [
    ( name = "workers-sdk-vendor-test",
      worker = (
        modules = [
          (name = "main.py", pythonModule = embed "python-workers-runtime-sdk.py"),
          %PYTHON_VENDORED_MODULES%
        ],
        compatibilityFlags = [%PYTHON_FEATURE_FLAGS, "enable_python_external_sdk", "disable_python_no_global_handlers"],
      )
    ),
  ],
);
