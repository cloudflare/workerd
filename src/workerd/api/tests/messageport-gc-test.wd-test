# Standalone repro for a MessagePort.postMessage() segfault. See repro.js.
#
#   workerd test repro.wd-test

using Workerd = import "/workerd/workerd.capnp";

const config :Workerd.Config = (
  services = [(
    name = "messageport-peer-collected-uaf",
    worker = (
      modules = [
        (name = "worker", esModule = embed "messageport-gc-test.js"),
      ],
      compatibilityFlags = ["expose_global_message_channel", "unsafe_module"],
    ),
  )],
);
