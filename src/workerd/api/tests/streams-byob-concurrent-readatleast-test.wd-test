using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [
    ( name = "streams-byob-concurrent-readatleast-test",
      worker = (
        modules = [
          (name = "worker", esModule = embed "streams-byob-concurrent-readatleast-test.js")
        ],
        compatibilityFlags = ["nodejs_compat", "streams_enable_constructors"],
      )
    ),
  ],
);
