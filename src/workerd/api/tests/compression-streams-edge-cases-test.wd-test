using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [
    ( name = "compression-streams-edge-cases-test",
      worker = (
        modules = [
          (name = "worker", esModule = embed "compression-streams-edge-cases-test.js")
        ],
        compatibilityFlags = ["nodejs_compat", "streams_enable_constructors", "transformstream_enable_standard_constructor"],
      )
    ),
  ],
);
