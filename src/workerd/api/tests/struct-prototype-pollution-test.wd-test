using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [
    ( name = "struct-prototype-pollution-test",
      worker = (
        modules = [
          (name = "worker", esModule = embed "struct-prototype-pollution-test.js")
        ],
        compatibilityFlags = ["nodejs_compat"],
      )
    ),
  ],
);
