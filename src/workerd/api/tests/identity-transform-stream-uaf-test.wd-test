using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [
    ( name = "identity-transform-stream-uaf-test",
      worker = (
        modules = [
          (name = "worker", esModule = embed "identity-transform-stream-uaf-test.js")
        ],
        compatibilityFlags = ["nodejs_compat"],
      )
    ),
  ],
);
