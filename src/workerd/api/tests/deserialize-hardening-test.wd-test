using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [
    ( name = "deserialize-hardening-test",
      worker = (
        modules = [
          (name = "worker", esModule = embed "deserialize-hardening-test.js")
        ],
        bindings = [
          ( name = "SERVICE", service = "deserialize-hardening-test" ),
        ],
        compatibilityFlags = ["nodejs_compat", "service_binding_extra_handlers"],
      )
    ),
  ],
);
