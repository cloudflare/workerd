using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [(
    name = "stdio-writesync-reentry-uaf-test",
    worker = (
      modules = [
        (name = "worker", esModule = embed "stdio-writesync-reentry-uaf-test.js"),
      ],
      compatibilityFlags = ["nodejs_compat_v2", "enable_nodejs_fs_module"],
    ),
  )],
);
