using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [
    ( name = "connect-neuter-test",
      worker = (
        modules = [
          (name = "worker", esModule = embed "connect-neuter-test.js"),
        ],
        compatibilityFlags = ["nodejs_compat_v2", "experimental"],
        bindings = [
          (name = "SELF", service = "connect-neuter-test"),
        ],
      )
    ),
  ],
);
