using Workerd = import "/workerd/workerd.capnp";

# The worker is instantiated twice so that the bindings below cross an isolate boundary.
# Only the first service's test() runs; see the filter argument in BUILD.bazel.
const unitTests :Workerd.Config = (
  services = [
    ( name = "queue-producer-metadata-test", worker = .worker ),
    ( name = "queue-producer-metadata-test-target", worker = .worker ),
  ],
);

const worker :Workerd.Worker = (
  modules = [
    ( name = "worker-producer-metadata-test", esModule = embed "queue-producer-metadata-test.js" )
  ],
  bindings = [
    ( name = "QUEUE", queue = "queue-producer-metadata-test-target" ),
  ],
  compatibilityFlags = ["nodejs_compat", "queues_json_messages", "capture_async_api_throws"],
);
