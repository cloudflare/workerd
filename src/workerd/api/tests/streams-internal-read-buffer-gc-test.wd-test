using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  v8Flags = ["--expose-gc"],
  services = [(
    name = "streams-internal-read-buffer-gc-test",
    worker = (
      modules = [
        (name = "worker", esModule = embed "streams-internal-read-buffer-gc-test.js"),
      ],
      compatibilityFlags = ["nodejs_compat_v2"],
    ),
  )],
);
