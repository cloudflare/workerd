using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [
    ( name = "structuredclone-error-serialize-test",
      worker = (
        modules = [
          (name = "worker", esModule = embed "structuredclone-error-serialize-test.js")
        ],
        bindings = [
          ( name = "SERVICE", service = "structuredclone-error-serialize-test" ),
        ],
        compatibilityFlags = ["nodejs_compat", "service_binding_extra_handlers"],
      )
    ),
  ],
);
