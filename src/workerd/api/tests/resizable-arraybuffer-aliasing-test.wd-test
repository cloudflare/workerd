using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [
    ( name = "resizable-arraybuffer-aliasing-test",
      worker = (
        modules = [
          (name = "worker", esModule = embed "resizable-arraybuffer-aliasing-test.js")
        ],
        compatibilityFlags = ["nodejs_compat", "websocket_standard_binary_type"],
      )
    ),
  ],
);
