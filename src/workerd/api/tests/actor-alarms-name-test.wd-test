using Workerd = import "/workerd/workerd.capnp";

const config :Workerd.Config = (
  services = [
    (name = "main", worker = .mainWorker),
    (name = "TEST_TMPDIR", disk = (writable = true)),
  ],
);

const mainWorker :Workerd.Worker = (
  compatibilityFlags = ["experimental", "nodejs_compat", "unsafe_module"],

  modules = [
    (name = "worker", esModule = embed "actor-alarms-name-test.js"),
  ],

  durableObjectNamespaces = [
    (className = "AlarmNameObject", uniqueKey = "6f8d9e0a1b2c3d4e5f60718293a4b5c6"),
  ],

  durableObjectStorage = (localDisk = "TEST_TMPDIR"),

  bindings = [
    (name = "NS", durableObjectNamespace = "AlarmNameObject"),
  ],
);
