using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [(
    name = "urlpattern-regex-search-oob-test",
    worker = (
      modules = [
        (name = "worker", esModule = embed "urlpattern-regex-search-oob-test.js"),
      ],
      compatibilityFlags = ["nodejs_compat", "urlpattern_standard"],
    ),
  )],
);
