using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  v8Flags = ["--expose-gc"],
  services = [
    ( name = "streams-test",
      worker = (
        modules = [
          (name = "worker", esModule = embed "streams-test.js")
        ],
        compatibilityFlags = ["nodejs_compat", "internal_stream_byob_return_view", "streams_enable_constructors", "transformstream_enable_standard_constructor"],
        bindings = [
          (name = "subrequest", service = "streams-test")
        ]
      )
    ),
  ],
);
