using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [
    ( name = "strip-types-test",
      worker = (
        modules = [
          (name = "worker", esModule = embed "strip-types-test.js")
        ],
        compatibilityDate = "2025-08-01",
        compatibilityFlags = ["nodejs_compat"],
      )
    ),
  ],
);
