using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [
    ( name = "http2-test",
      worker = (
        modules = [
          (name = "worker", esModule = embed "http2-test.js")
        ],
        compatibilityDate = "2025-07-01",
        compatibilityFlags = ["nodejs_compat", "enable_nodejs_http2_module"]
      )
    ),
  ],
);
