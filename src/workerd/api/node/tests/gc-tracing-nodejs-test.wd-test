using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  v8Flags = ["--expose-gc"],
  services = [
    ( name = "gc-tracing-nodejs-test",
      worker = (
        modules = [
          (name = "worker", esModule = embed "gc-tracing-nodejs-test.js")
        ],
        compatibilityFlags = ["experimental", "nodejs_compat", "nodejs_compat_v2", "nodejs_zlib", "enable_weak_ref"],
      )
    ),
  ],
);
