using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [
    ( name = "source-phase-new-registry-test",
      worker = (
        modules = [
          (name = "worker", esModule = embed "source-phase-new-registry.js"),
          (name = "wasm", wasm = embed "test.wasm"),
        ],
        compatibilityDate = "2025-07-01",
        compatibilityFlags = [
          "nodejs_compat",
        ],
        # Enable the new module registry
        modulesRoot = ".",
        bindings = [],
        durableObjectNamespaces = [],
        durableObjectStorage = ( inMemory = void ),
      )
    ),
  ],
);